import { SearchWorkerPipe } from './search-worker.pipe';

describe('SearchWorkerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkerPipe();
    expect(pipe).toBeTruthy();
  });
});
