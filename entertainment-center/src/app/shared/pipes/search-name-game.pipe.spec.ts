import { SearchNameGamePipe } from './search-name-game.pipe';

describe('SearchNameGamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchNameGamePipe();
    expect(pipe).toBeTruthy();
  });
});
